{
  "name": "tcp-chat-client",
  "version": "1.0.0",
  "description": "TCP Chat CLI client",
  "main": "dist/client.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/client.js",
    "dev": "ts-node-dev --respawn --transpile-only src/client.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
